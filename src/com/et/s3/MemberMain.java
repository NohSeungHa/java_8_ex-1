package com.et.s3;

public class MemberMain {

	public static void main(String[] args) {
		//메서드  오버로딩
		Point p = new Point();
		p.hap(10, 15);
		
		/*Member m = new Member();	//Member()는 메서드로 침 
		m.info();					//m.Member()는 호출안됨 처음 생성할때만 사용되고 그뒤로 사용못함
		Member m2 = new Member();
		m2.info();
		Point p = new Point();
		p.kor = 50;
		p.eng = 50;
		System.out.println(p.total);	//위에서 객체생성을 하면서 이미 더해서 값을 가지고 있으므로 0
		System.out.println(p.avg);		//객체생성시 한번만 나누고 끝 연산을 하려면 메서드 만들어서 해야함
*/	}

}
